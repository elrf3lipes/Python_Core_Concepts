def raise_to_power(base_num, pow_num): #getting two pieces of info
    result = 1 #defining a variable to store the result of doing the math
    for index in range (pow_num): #loop through this pow_num(starting from 0 will loop pow_num *times)number_grid = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9],
    [0]
]

print(number_grid[0][1])
        result = result * base_num
    return result

print(raise_to_power(2, 3))